# Gate 14B Artifacts

Verification artifacts for the macOS app shell (Gate 14B).

- `launch.png`, `post10s.png` — UI screenshots (PTH captures locally; the CI/agent
  session has no window-server/screen-capture access).
- `playback.mov` — ≥30s screen recording of a play session (PTH).

The session could not capture screenshots ("could not create image from display").
See `../MACOS_PLAYABLE_VERIFICATION.md` for programmatic results and the manual
checklist. hardware_validated: false.
